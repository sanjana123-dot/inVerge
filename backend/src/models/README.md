# Models

Data models are defined in `prisma/schema.prisma` and accessed via Prisma Client (`src/config/database.ts`).

Business entities: User, Startup, ConnectionRequest, Endorsement, Message, Notification, ActivityLog, TrustScoreHistory.
